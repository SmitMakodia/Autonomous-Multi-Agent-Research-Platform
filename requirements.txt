# AgentForge dependencies, pinned to the set verified working on 2026-07-26.
#
# IMPORTANT - install PyTorch FIRST and from the CUDA 12.8 index, not from PyPI:
#
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# The cu121 build referenced by the previous version of this file has no sm_120 kernels and
# will not run on an RTX 50-series (Blackwell) GPU: torch imports, reports
# cuda.is_available() as True, then fails at the first kernel launch. setup_agentforge.bat
# installs from the right index and verifies it with a real GPU matmul before finishing.
#
#     torch==2.11.0+cu128
#     torchvision==0.26.0+cu128

# --- Web framework -----------------------------------------------------------
fastapi==0.140.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
sse-starlette==3.4.6
aiofiles==25.1.0

# --- HTTP and scraping -------------------------------------------------------
httpx==0.28.1
Crawl4AI==0.9.2
beautifulsoup4==4.15.0
markdownify==1.2.3
ddgs==9.14.4              # the code prefers this over the deprecated duckduckgo-search

# --- AI / ML / embeddings ----------------------------------------------------
transformers==5.14.1
sentence-transformers==5.6.1
accelerate==1.14.0        # required by transformers for device_map="cuda" in the OCR agent
tiktoken==0.13.0
rank-bm25==0.2.2
pillow==12.2.0
numpy==2.4.4

# --- Document parsing --------------------------------------------------------
# All four are imported by backend/agents/file_read_agent.py but were absent from the
# previous requirements file, so a clean install could not read PDF/DOCX/PPTX/XLSX at all.
pymupdf==1.28.0
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5

# --- Benchmarks, tests, evidence capture -------------------------------------
nvidia-ml-py==13.610.43   # GPU memory sampling for the VRAM benchmark
matplotlib==3.11.1        # figure generation
pytest==9.1.1
pytest-asyncio==1.4.0
playwright==1.61.0        # also pulled in by Crawl4AI; drives the screenshot capture
